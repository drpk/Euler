package me.pramukh.euler.eulerfour;

public class PalindromeNumbers {

    public boolean checkIfPalindrome(int n){
        String s = String.valueOf(n);
        for (int i = 0; i < s.length(); i++) {
            int last = s.lastIndexOf(s);

        }
        return true;
    }
}
